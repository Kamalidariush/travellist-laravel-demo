apiVersion: apps/v1
kind: Deployment
metadata:
  name: travellist-app
  labels:
    app: travellist-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: travellist-app
  template:
    metadata:
      labels:
        app: travellist-app
    spec:
      containers:
      - name: travellist-app
        image: "{{ image_id }}"
        ports:
          - containerPort: 80
        envFrom:
          - configMapRef:
              name: travellist-config
      imagePullSecrets:
        - name: nexus-docker
